import type { FastifyInstance } from 'fastify';

// Error handler middleware placeholder.
// Will implement structured error responses per EH-1 in Story 1.
// Every error is logged before being returned (EH-5).
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function registerErrorHandler(_app: FastifyInstance): void {
  // placeholder
}
