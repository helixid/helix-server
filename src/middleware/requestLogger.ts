import type { FastifyInstance } from 'fastify';

// Request logger middleware placeholder.
// Will add requestId and structured logging in Story 1.
// SA-8: nothing sensitive (keys, raw VCs, raw VPs) must appear in logs.
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function registerRequestLogger(_app: FastifyInstance): void {
  // placeholder
}
