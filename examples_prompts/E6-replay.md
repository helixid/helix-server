Write a HelixID example showing VP replay protection for a service provider. The example should use sdk.verifyVP() for cryptographic verification, then check the returned vpId against a local store before accepting the request. If vpId already exists in the store, reject with a replay error. If not, write it with a TTL matching the VP expiry. Show this as Express middleware. Use TypeScript.